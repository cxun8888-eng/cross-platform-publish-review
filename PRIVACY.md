# 隐私说明

扩展只在 manifest 声明的官方发布域名读取 URL fragment 交接的标题、正文和标签、当前页面状态，以及发布后可见的公开作品链接和公开指标。

扩展不读取 Cookie、密码、验证码、浏览历史或其他站点内容；不读取或自动选择本地文件；不保存平台登录凭据。

草稿读取后从地址栏清除，并在当前标签 `sessionStorage` 中最多保留 10 分钟用于刷新恢复。应用 Origin 与回调路径长期保存在扩展本地，直至用户修改、清除扩展数据或卸载扩展。

Celery 任务结果默认保留 24 小时；SQLite 中的作品、幂等任务记录和指标快照默认不会自动删除。生产部署者必须根据自身隐私政策和法律要求增加保留期、删除与导出机制。

RedFox API Key 只应存在于 Worker 环境。Web、扩展和 API 进程不需要也不应获得该 Key。
